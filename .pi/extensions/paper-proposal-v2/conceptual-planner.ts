import type { EditPlan,LocalContext,ResolvedIntent,SemanticEditPlanner,TargetCandidate } from './types.js';
import { instructionHash } from './intent-resolver.js';
export type ConceptualRevisionPlan=EditPlan&{intent:'CONCEPTUAL_REVISION';targetSectionIds:string[];relatedEntryIds:string[];scientificGoal:string;assumptions:string[];proposedActions:string[];expectedEffects:string[]};
export async function buildConceptualPlan(intent:ResolvedIntent,sha:string,target:TargetCandidate,context:LocalContext,planner:SemanticEditPlanner){const proposal=await planner.plan({intent,instruction:intent.evidence[0],target,context,constraints:['Return bounded replace actions only; do not return a complete paper.'],documentSha256:sha});const actions=proposal.actions??[];if(actions.length===0||actions.length>3)throw new Error('CONCEPTUAL_PATCH_BUDGET_EXCEEDED');if(actions.some(a=>a.kind!=='replace'||!context.fragments.some(f=>f.entryId===(a as any).targetEntryId)))throw new Error('CONCEPTUAL_SCOPE_VIOLATION');return {plan:{planVersion:'2',documentSha256:sha,intent:'CONCEPTUAL_REVISION',instructionHash:instructionHash(intent.evidence[0]),resolvedTargets:[target.entryId],targetSectionIds:[target.entryId],relatedEntryIds:context.fragments.map(f=>f.entryId),scientificGoal:intent.requestedEffect??intent.evidence[0],assumptions:[],constraints:intent.constraints,proposedActions:[],semanticChange:true,destructiveIntent:false,cleanupLevel:'NONE',actions,expectedEffects:proposal.expectedEffects??[],unresolvedQuestions:proposal.unresolvedQuestions??[]} as ConceptualRevisionPlan,modelCalls:1};}
