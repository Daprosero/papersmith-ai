---
description: "Trigger: ingest reference PDFs into high-fidelity Markdown (LaTeX equations, Markdown tables, figures as files) using Marker, locally and keyless."
---

Read `skills/paper-ingestion/SKILL.md` and follow it for this task. Treat the text below as `$ARGUMENTS`:

$ARGUMENTS
