---
description: Auditar una skill o CLI (codigo real vs documentacion), solo informa
agent: build
---

Load the `skill-audit` skill via the skill tool and follow `.claude/skills/skill-audit/SKILL.md` exactly. Reports; never repairs.

Requires a shell: without bash, refuse outright (no read-only fallback). Derive both halves by execution (subject's own refusal + documented table), slice subjects larger than one closed surface, and ask slicing questions via the question tool. Delegate the report stretch to `@audit-report`.
