---
description: Convertir el protocolo experimental en codigo y correr sus mediciones
agent: build
---

Load the `experimental-implementation` skill via the skill tool and follow `.claude/skills/experimental-implementation/SKILL.md` exactly.

Steps: resolve both documents (the experiments protocol plus the mathematical proposal it answers to), run structure before materialization, delegate build to `@experiments-build` and walk to `@experiments-walk`. Stop at the rehearsal; the campaign launch is the operator's.
