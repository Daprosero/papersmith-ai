# Tasks: The pilot decides the remote strategy

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~420-480 (slice 1 ~60, slice 2 ~110, slice 3 ~250, docs ~40) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (placement) -> PR 2 (classification) -> PR 3 (proposal + 8th key) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Rehearsal fetch placement + `--dest` pairing guard | PR 1 | `python3 -m unittest tests.test_remote_execution -v` | `remote_cli.py fetch --smoke` against a scratch target dir | `remote_cli.py` diff only; revert restores prior `--dest`-always behavior |
| 2 | Classification seam (`impl_execution_strategy.py`, widened rows, `--local-budget-seconds`) | PR 2 | `python3 -m unittest tests.test_proposal_implementation tests.test_implementation_core -v` | `implementation_cli.py probe` against a scratch target with one job folder | New module + additive fields; revert deletes the module and two read sites, refuses nothing |
| 3 | Proposal event, `--elect`, 8th binding key, `SUPERSEDED` | PR 3 | `python3 -m unittest tests.test_proposal_implementation -v` | `implementation_cli.py propose` then `gate` against a scratch position ledger | Schema-invalidating; revert restores 7-key digest, any token minted under 8 keys must be re-minted |

## Phase 0: MEASURE (blocks every downstream task)

- [x] 0.1 Run `python3 -m unittest tests.test_skill_audit -v -k rule_b_finds_no_target_vocabulary_in_the_forge` (and the full vocabulary suite in that file) with the new identifiers not yet introduced, to get the pre-change baseline green. Then re-run after slices 1-3 land, before merge. Verification: suite passes both times; red-before-added-identifiers-would-be-a-tooling-bug, so the real gate is the post-change run. DONE for slice 1: `ForgeVocabularyDerivedGuardTests` (8 tests, `tests/test_proposal_implementation.py`) reconfirmed green with `rehearsal` now live in code. Full 3-slice re-run still pending (Phase 2/3 not started).
- [x] 0.2 Grep `tests/test_remote_execution.py` and `.claude/skills/remote-execution/scripts/remote_cli.py` for existing `submission_id` shape validation (regex, `Path.resolve()` containment check, or refusal on `..`/absolute paths) around `cmd_fetch` and `cmd_poll`. Record the finding as a one-line note in the PR 1 description: "new guard" or "duplicate of `<symbol>`, do not add a second validator." Verification: grep output quoted in the PR description; no code changes in this task. FINDING: new guard — 56 occurrences of `submission_id` across the skill, zero shape validation anywhere.
- [x] 0.3 Do not start task 6.1 (path-traversal guard) or write the threat-matrix RED test until 0.2's finding is recorded.

## Phase 1: Rehearsal Placement (Slice 1 — PR 1, independent, closes the live artifact-leak hole)

- [x] 1.1 RED: in `tests/test_remote_execution.py`, add a test asserting `cmd_fetch(..., smoke=True, dest=<non-None>)` refuses via `RemoteCLIError` with distinct wording, before any filesystem call (assert on a non-existent `--target` to prove ordering). Mutation-proven: moving the pairing check below `target.is_dir()` must turn this red — verified below, then reverted. Delete `__pycache__` for `remote_cli` before re-running.
- [x] 1.2 RED: add a test asserting `cmd_fetch(..., smoke=False, dest=None)` refuses via `RemoteCLIError` with distinct wording, before any filesystem call. Mutation-proven: deleting the second half of the pairing check must turn this red — verified below, then reverted.
- [x] 1.3 GREEN: in `.claude/skills/remote-execution/scripts/remote_cli.py`, add the pure-argv pairing check in `cmd_fetch` above `target = Path(target).resolve()`. Run 1.1-1.2, confirm green.
- [x] 1.4 In `remote_cli.py`, add `REHEARSAL_DIRNAME = "rehearsal"` beside the existing `QUARANTINE_DIRNAME = "quarantine"` (line ~196).
- [x] 1.5 In `remote_cli.py`'s fetch parser (`fetch.add_argument("--dest", ...)`, line ~2326), drop `required=True`. Also required a downstream sync fix: `proposal-implementation/SKILL.md`'s seam table `fetch` row listed `--dest` as required, enforced by `GuidingTableFlagsTests`; dropped it there too.
- [x] 1.6 RED: add a test that `fetch --smoke` (no `--dest`) lands under `target/product/LEDGER_DIRNAME/REHEARSAL_DIRNAME/<submission_id>`, not under `Results/shards/`. Mutation-proven: making the `smoke` branch fall through to `Path(dest)` must turn this red — verified below, then reverted.
- [x] 1.7 GREEN: in `cmd_fetch`'s placement decision (line ~1629 area), add the `smoke` branch computing `final_dest` under `REHEARSAL_DIRNAME`; leave the `verdict == "current"` and quarantine branches unchanged. Run 1.6, confirm green.
- [x] 1.8 Verify the 8-step contract survives unchanged: re-run the existing `.partial/`, `--force`, `os.replace`, ledger-write-last tests in `tests/test_remote_execution.py` against a rehearsal fetch (`smoke=True`). No new task if they already parametrize `dest`; otherwise add one parametrized case per step. DONE: existing `FetchTests` needed no changes (they never pass `smoke=True`); added `test_rehearsal_fetch_preserves_the_8_step_ordering_contract`.
- [x] 1.9 RED then GREEN — path-traversal guard from 0.2's finding: only if 0.2 found no existing check, add a test that `submission_id` containing `..` or an absolute-path shape causes `fetch --smoke` to refuse before `adapter.fetch()`, with `final_dest.resolve()` asserted to stay under `target/product/LEDGER_DIRNAME`. Mutation-proven: removing the `.resolve()` containment check must turn this red. If 0.2 found an existing check, skip this task and note the duplicate finding in the PR instead. DONE: guard also covers the pre-existing quarantine branch (design D7 already frames quarantine as the branch this hole was reachable through before rehearsal existed).
- [x] 1.10 Verification command for the whole slice: `python3 -m unittest tests.test_remote_execution -v`. State what red looked like before green: 1.1/1.2 failed with no refusal raised (fetch proceeded to `target.is_dir()`); 1.6 failed with the artifact written under `Results/shards/`. Full assigned suite (`tests.test_proposal_implementation tests.test_skill_audit tests.test_remote_execution`): 1635 tests, OK, skipped=2.

## Phase 2: Classification Seam (Slice 2 — PR 2, additive, refuses nothing yet)

- [x] 2.1 In `.claude/skills/remote-execution/scripts/jobfolder.py`, add `--local-budget-seconds` handling in `generate_job` mirroring the existing `accelerator` conditional block: write `run_config["localBudget"] = {"seconds": N}` only when the flag is passed; omission writes no key. Extend `validate_run_config` to accept the optional `localBudget` block. FINDING: `validate_run_config` needed no code change — it checks `REQUIRED_RUN_CONFIG_FIELDS` against a fixed list and rejects nothing outside it (no allowlist exists to widen), the same reason `accelerator`/`environment` needed none either. Documented in that function's own docstring instead of adding a no-op guard; one test (`test_validate_run_config_accepts_a_localbudget_block`) proves the round-trip rather than mutation-proving a rejection that was never written.
- [x] 2.2 RED: test that `generate-job` without `--local-budget-seconds` produces a `run-config.json` with no `localBudget` key (not a default value). Mutation-proven: substituting a default `{"seconds": 0}` must turn this red. Verified red (localBudget: {"seconds": 0} present), then reverted.
- [x] 2.3 GREEN: confirm 2.1 satisfies 2.2. Add the CLI flag to `generate_job`'s argparse subparser in `implementation_cli.py` if not already routed there. FALSE PREMISE: `generate-job`'s argparse subparser and its `JOBFOLDER.generate_job(...)` call both live in `remote_cli.py` (`_build_parser()`/`main()`), not in `implementation_cli.py` — grep confirms zero `generate-job`/`generate_job` occurrences in `implementation_cli.py`. `--local-budget-seconds` was wired in `remote_cli.py` beside `--accelerator-*`/`--environment-*`, matching where every sibling flag for this exact command actually lives.
- [x] 2.4 Create `.claude/skills/_core/implementation/impl_execution_strategy.py`, sibling to `impl_availability.py`: `classify_remote_necessity(*, jobs, results_status, cost_forecast)`, pure, keyword-only, no defaults, no I/O, never raises. Implement the 5-rule ordered ladder from design D3 (results current -> local-sufficient; accelerator declared -> must-remote; budget exceeded -> must-remote; budget within -> local-sufficient; else -> optional with a `reason`). Rule 5's own three-reason sub-priority (undocumented by design beyond a pipe-separated list) was resolved as: no measurement at all (`results.unmeasured`, for `results_status` in `{"absent", "unreadable"}`) outranks an undeclared budget (`budget.undeclared`), which outranks a declared budget with no projectable forecast (`forecast.unprojectable`) — stated explicitly in the module's own docstring/comments.
- [x] 2.5 RED: one test per verdict class and per `reason` value (`results.current`, `accelerator.declared`, `budget.exceeded`, `budget.within`, `budget.undeclared`, `forecast.unprojectable`, `results.unmeasured`). Mutation-proven per row: invert rule 3's `>`; delete rule 1's short-circuit; make rule 5 fall through to `local-sufficient` — each verified red, then reverted. Tests live in `tests/test_implementation_core.py::ClassifyRemoteNecessityTests` (sibling to `LaunchAvailableTests`), not `test_proposal_implementation.py` — the pure module's natural home.
- [x] 2.6 RED: test that calling `classify_remote_necessity` with `results_status` omitted (relying on a default) raises `TypeError`. Mutation-proven: giving `results_status` a default value must turn this red. Verified red (`TypeError not raised`), then reverted. Also covered `cost_forecast`/`jobs` omission for completeness (not required by this task, added for symmetry).
- [x] 2.7 GREEN: run 2.5-2.6 against 2.4's implementation, confirm all green. Delete `__pycache__/impl_execution_strategy*` before the mutation-proof re-runs. 16 tests green (`ClassifyRemoteNecessityTests` + `ExecutionStrategyNoUpwardImportsTests`, the latter mirroring `LaunchAvailableNoUpwardImportsTests`).
- [x] 2.8 In `implementation_cli.py`, widen `remote_execution_jobs_state()`'s per-job row to carry `accelerator` and `localBudget` read from the already-open `run_config` in its existing `_discovered_job_folders` loop (no second `JOBFOLDER.read`). Updated one pre-existing strict key-set assertion (`RemoteExecutionJobsSectionTests::test_two_jobs_across_two_services_one_stale`, line ~4888) that enumerated the row's exact key set and would otherwise have failed on this additive widening.
- [x] 2.9 Widen `_position_write_evidence`'s return to carry `jobs` (not only `["smokeReady"]`), sourced from the same `remote_execution_jobs_state()` call it already makes.
- [x] 2.10 Wire `probe`'s report to call `classify_remote_necessity` with the widened rows plus `results_status`/`cost_forecast`, and print the per-job verdict and summary. Landed under `remoteExecution.necessity` (probe has no separate print step — `main()` JSON-dumps the whole return dict, so inclusion in the returned structure IS the surfacing). `cost_forecast` (`search_cost_forecast(...)`) is now computed once and reused for both `search.costForecast` and the classify call, rather than twice.
- [x] 2.11 RED: test that `probe` against a scratch target with one job folder (no accelerator, no localBudget, non-current results) reports `optional` with `reason` naming the missing fact. Mutation-proven: same as 2.5's rule-5 mutation, exercised through the CLI this time. Verified red through the CLI subprocess harness, then reverted. Fixture's `results_status` is `"absent"` (no `Probe_results.json` written), so the observed reason is `results.unmeasured`, not `budget.undeclared` — both are valid rule-5 outcomes; `results.unmeasured` is correct per 2.4's stated sub-priority.
- [x] 2.12 GREEN: confirm 2.10's wiring satisfies 2.11.
- [x] 2.13 Verification command for the whole slice: `python3 -m unittest tests.test_proposal_implementation tests.test_implementation_core -v`. State what red looked like before green: 2.2 failed with a `localBudget: {"seconds": 0}` key present; 2.5/2.6 failed with `TypeError` not raised or wrong verdict/reason; 2.11 failed with no `optional` verdict printed. RAN (via `.venv/bin/python`, never bare `python3` — see the interpreter note that opened this session): assigned suite `tests.test_proposal_implementation tests.test_skill_audit tests.test_remote_execution` — **1643 tests, OK, skipped=2** (baseline after Phase 1 was 1635; +8 new: 7 in `LocalBudgetDeclarationTests`, 1 in `ProbeReportedFactsRosterTests`). `tests.test_implementation_core` (not in the orchestrator's assigned 3-suite command, but named by this task and the natural home of the new pure module's own unit tests, per its `LaunchAvailableTests` precedent) — **73 tests, OK** (57 baseline + 16 new).

## Phase 3: Proposal Event + 8th Binding Key (Slice 3 — PR 3, schema-invalidating, last)

- [x] 3.1 Structural test (required, not optional — do this before any binding-key code change): add a test in `tests/test_proposal_implementation.py` asserting `_AUTHORIZATION_BINDING_KEYS`, `_authorization_binding`'s returned dict keys, and `cmd_gate`'s inline `gate_binding` dict keys are all exactly `set(_AUTHORIZATION_BINDING_KEYS)`. Mutation-proven: adding a key to only `_authorization_binding` (not the other two) must turn this red. DONE: `AuthorizationBindingKeysStructuralTests` (AST-based, reusing the file's own `returned_keys()` helper for the return-dict half and a direct `ast.Assign` walk for `cmd_gate`'s local `gate_binding`). Mutation (`"bogus": None` added only to `_authorization_binding`'s return) verified red, then reverted.
- [x] 3.2 GREEN (trivial today): confirm 3.1 passes against the current 7-key set before any schema change — this proves the test itself is reachable, not vacuous. DONE — ran green against the 7-key set before 3.6 added the 8th.
- [x] 3.3 Implement `cmd_propose` in `implementation_cli.py`: appends `{"kind": "proposal", "digest", "jobs", "workers", "dependsOn", "rationale", "campaign": {"commit", "jobSet"}, "session", "at", "proposeOrdinal"}` via `impl_position.append_event`. `rationale` required, non-blank, human-authored (same discipline as `justification`). `proposeOrdinal` counts prior `proposal` events. DONE. CLI argv: `--job` (repeatable, required, ≥1 — the campaign's declared job subset), `--worker` (repeatable, required, ≥1 — write-only history), `--depends-on job:dependency` (repeatable, optional), `--rationale` (required, `-` for stdin). No `--revision`. Refuses `EMPTY_RATIONALE` on blank (new code, not itemized in design's D1 table — that table scopes `gate`'s own new codes; `propose`'s own precondition needed a name and follows this file's own `EMPTY_JUSTIFICATION` precedent).
- [x] 3.4 RED: test that `propose` for a campaign with jobs A and B writes one proposal event naming both jobs, dependency edges, and rationale. DONE: `ProposeCommandTests.test_propose_writes_one_event_naming_every_field`.
- [x] 3.5 GREEN: confirm 3.3 satisfies 3.4. DONE.
- [x] 3.6 Add `proposalDigest` as the 8th `_AUTHORIZATION_BINDING_KEYS` entry, engine-derived on both sides (never argv) from the newest matching `proposal` event's digest. Update all three literals from 3.1 together in this task. **FALSE PREMISE, corrected**: "newest proposal naming this job" (job-name filtering) makes `GATE_PROPOSAL_MISMATCH` structurally unreachable — a token minted for job X can only ever bind to a proposal that already names X (by the lookup's own construction), and `_verify_gate_authorization`'s pre-existing `jobName` check already refuses before `_verify_gate_proposal` runs if the token is used for a different job. Corrected to "newest proposal whose OWN `campaign` equals the CURRENT `_campaign_identity()`" (campaign-identity filtering, never job-name filtering) — every job a campaign proposal covers binds to the SAME proposal, matching "gate --unit is already campaign-scoped." This makes MISMATCH genuinely reachable (a job outside the bound proposal's own `jobs` list) and is proven by `test_gate_refuses_proposal_mismatch_when_the_bound_proposal_omits_the_job`, which needed a SECOND, later proposal on the ledger to give a job-name-based mutation something to wrongly accept — see that test's own comment.
- [x] 3.7 RED (re-run 3.1 with the 8th key added): confirm the structural test still passes — this is the enforcement, not a new test. DONE, green.
- [x] 3.8 Implement `_proposal_digest` and `_verify_gate_proposal` (`GATE_PROPOSAL_UNKNOWN` -> `GATE_PROPOSAL_MISMATCH` -> `GATE_PROPOSAL_STALE`), placed immediately after `_verify_gate_authorization`, before `append_event` for the `gate` event — never at the top of the ladder. DONE. `gate_binding`'s own `proposalDigest` entry is computed (for structural key-set parity, task 3.1) but deliberately never compared against the record — `_verify_gate_proposal` verifies against the record's own frozen value exclusively; documented inline beside `_AUTHORIZATION_BINDING_KEYS` and at the `gate_binding` literal.
- [x] 3.9 RED x3, one per code — all three verified red-for-the-right-reason then reverted:
  - `GATE_PROPOSAL_UNKNOWN`: **mutation description corrected in execution** — "drop the `is None` branch" alone changes nothing observable (no proposal event ever carries a `digest: null`, so the guard is a no-op for correctness, only a short-circuit). The MEANINGFUL mutation actually proven red: treat `proposal_digest is None` as "match any proposal event" instead of refusing — required a SECOND, later-published proposal on the ledger (frozen token predates it) to give the mutation something to wrongly accept.
  - `GATE_PROPOSAL_MISMATCH`: widened to `any(job in e["jobs"] for e in ALL proposal events)` — verified red (same "needs a second proposal on disk" fixture shape as above).
  - `GATE_PROPOSAL_STALE`: campaign comparison narrowed to `commit` only (jobSet dropped) — verified red.
  Deleted `__pycache__` for `implementation_cli`/`_core/implementation` before each mutation re-run.
- [x] 3.10 GREEN: confirm 3.8 satisfies 3.9's three cases. DONE.
- [x] 3.11 RED: proposal survives a same-campaign retry — a failed job A's retry re-verifies against the same proposal event, no re-propose needed. DONE: `test_proposal_survives_a_same_campaign_retry_with_no_re_propose` (propose once, gate job1 successfully — consuming its token — then mint a SECOND token via `offer` with no second `propose`, gate again, both succeed, exactly one `proposal` event on the ledger). Mutation-proven: comparing `proposal["campaign"]` against `{**campaign, "positionStatus": record.get("positionStatus")}` (any extra key the stored event never carried) breaks even the FIRST gate call — verified red, then reverted.
- [x] 3.12 GREEN: confirm no code change is needed beyond 3.8's correct key set. DONE, no fix needed — 3.8's `{commit, jobSet}`-only comparison was already correct.
- [x] 3.13 Implement `--elect <job>` (repeatable) on `gate`'s argparse subparser and `_verify_optional_election` (`GATE_ELECTION_REQUIRED` -> `GATE_ELECTION_MISMATCH`). **Resolved ambiguity**: design's "every job IN THE UNIT that classifies optional" reuses "unit" loosely — `gate` authorizes exactly one job per call (`--job`, never repeatable, even in `--unit` campaign mode, where `--unit` is an UNRELATED opaque work-unit list `packer.distribute()` treats as opaque, not job names). Resolved "the unit" as "the one job this call is gating" (`args.job`): REQUIRED fires when that job classifies `optional` and `--elect` doesn't name it; MISMATCH fires when `--elect` names anything OTHER than that job, or names it while it does not classify `optional`. `elected: [...]` recorded on the `gate` event (additive).
- [x] 3.14 RED x2, both verified red then reverted:
  - `GATE_ELECTION_REQUIRED`: default `--elect` to `[job]` when omitted — verified red (rc 0 instead of 2).
  - `GATE_ELECTION_MISMATCH`: dropped the not-in-unit / not-optional check — verified red (two fixtures: wrong job named alongside a valid one, and the valid job named while it classifies non-optional).
- [x] 3.15 GREEN: confirm 3.13 satisfies 3.14. DONE.
- [x] 3.16 Implement the D6 discriminator in `_verify_gate_authorization`: 8-key recompute match -> genuine; else `"proposalDigest" not in record` AND 7-key recompute matches -> `GATE_AUTHORIZATION_SUPERSEDED`; else `GATE_AUTHORIZATION_UNKNOWN` (unchanged). DONE, with the code comment stating `SUPERSEDED` is diagnostic only.
- [x] 3.17 RED: a legitimate pre-change 7-key token (no `proposalDigest`) must produce `GATE_AUTHORIZATION_SUPERSEDED`. DONE: `test_a_pre_change_seven_key_token_is_superseded_not_unknown` (hand-constructed 7-key event, digested exactly as `_find_or_mint_authorization` digests, over the pre-8th-key shape). Mutation-proven: deleting the 7-key fallback recompute collapses this to `GATE_AUTHORIZATION_UNKNOWN` — verified red, then reverted. Companion test `test_an_edited_post_change_token_stays_unknown_not_superseded` proves the measurement side (a genuine post-change event with `proposalDigest` merely deleted stays `UNKNOWN`, never `SUPERSEDED`).
- [x] 3.18 GREEN: confirm 3.16 satisfies 3.17. DONE.
- [x] 3.19 Regression RED-then-GREEN: the four existing authorization codes still fire in the original order. Mutation-proven: reordered `_verify_gate_proposal` above `_verify_gate_authorization` (via a naive direct-lookup `record`, since the real `record` isn't available before authorization verification runs) — 3 of the 4 existing tests (`UNKNOWN`/`MISMATCH`/`STALE`, which use `"unminted-placeholder"` tokens with no real authorization record) went red with `GATE_PROPOSAL_UNKNOWN` instead of their own code; the 4th (`CONSUMED`, whose fixture has a genuinely bound proposal) stayed green since the naive proposal check passes regardless of order for that specific scenario — a legitimate, not-a-gap result. Reverted; all four confirmed green again.
- [x] 3.20 Fixture migration. **Count confirmed at exactly 5** (matches design's audited count), but **FALSE PREMISE on SCOPE, corrected**: each of the 5 needed not only a `propose` call but ALSO `--elect <job>` — `job1` (the shared fixture job name throughout `GateCommandTests`/`OfferCommandTests`) declares no `accelerator` and no `localBudget`, and these fixtures never measure results, so `classify_remote_necessity` verdicts `optional` for it under the now-active D3+D5 ladder, and `GATE_ELECTION_REQUIRED` fires without an election. This is still confined to the SAME 5 fixtures (no 6th fixture touched, no re-inflation to 19) — the design's own file-count audit holds; only the per-fixture edit size was one line larger than the task described. Also updated the one prose-audited docstring inline (see 3.22).
- [x] 3.21 Confirm untouched by design (verification only). DONE — full suite run after 3.6-3.20 landed: the 14 `unminted-placeholder`, 3 minted-token `UNKNOWN`/`MISMATCH`/`STALE` (the `CONSUMED` one is inside the migrated 5 since its FIRST call needed `--elect`), 1 literal-digest, 2 handmade `authorization-consumed`, and 1 handmade tokenless `gate` fixtures all pass unmodified.
- [x] 3.22 Update the prose-audited test. DONE — `test_an_authorization_minted_before_a_later_contract_change_still_gates`'s docstring updated in place: `proposalDigest` (the 8th key) is a LEDGER fact, not a `__steps__` contract fact, so "`_AUTHORIZATION_BINDING_KEYS` names no contract fact" still reads true — stated explicitly rather than left implicit.
- [x] 3.23 Verification command for the whole slice: `python3 -m unittest tests.test_proposal_implementation -v`. RAN via `.venv/bin/python`: **738 tests, OK** (724 baseline after Phase 2 + 14 new). What red looked like before green, as actually observed (see 3.6/3.9/3.20 notes above for where the task's own mutation descriptions needed correcting): 3.1's structural test failed by construction (no test existed); 3.9's three GATE_PROPOSAL_* mutations each produced `returncode == 0` or a different code than expected; 3.11's retry mutation broke even the FIRST gate call; 3.14's two GATE_ELECTION_* mutations produced `returncode == 0`; 3.17's SUPERSEDED mutation collapsed to `GATE_AUTHORIZATION_UNKNOWN`; 3.19's reorder mutation broke 3 of 4 existing authorization-code tests; 3.20's five (now six-touching, see above) fixtures failed `GATE_PROPOSAL_UNKNOWN` then `GATE_ELECTION_REQUIRED` in sequence before both fixes landed.

## Phase 4: Documentation and Final Vocabulary Verdict

- [x] 4.1 Update `.claude/skills/remote-execution/SKILL.md` and `references/usage.md` (or equivalent) with `--dest` no longer required under `--smoke`, and the rehearsal placement behavior. DONE — no `references/` directory exists for this skill (confirmed on disk); `SKILL.md` is the equivalent, and its `fetch` bullet already needed the rehearsal-placement sentence Phase 1 had left undocumented at the skill-doc level (the CLI's own `--dest` help text was already current from Phase 1). Also added a bullet documenting `--local-budget-seconds`/`localBudget` beside the existing `accelerator` documentation (Phase 2 shipped the code but not this doc).
- [x] 4.2 Update `.claude/skills/proposal-implementation/SKILL.md` and its references with `propose`, `--elect`, `--local-budget-seconds` cross-reference, and the new/changed codes. DONE — `SKILL.md`'s command table gained a `propose` row and the `gate` row's refuses-on column now names all eight new/changed codes (`GATE_AUTHORIZATION_SUPERSEDED` plus the six `GATE_PROPOSAL_*`/`GATE_ELECTION_*` codes — the design's own count of "six" undercounted by naming `GATE_AUTHORIZATION_SUPERSEDED` separately from "six new/changed" in its own phrasing; all eight are documented). `references/usage.md` gained a `propose` section and gate-section updates (worked `--elect` invocation, updated refusal-code prose, a note that `GATE_PROPOSAL_STALE` is a separate mechanism from `_AUTHORIZATION_BINDING_KEYS`'s own "seven facts" prose so that older paragraph stays accurate rather than silently wrong). Documented plainly that a job with no `accelerator` and no `--local-budget-seconds` classifies `optional` and needs `--elect` at every gate.
- [x] 4.3 Re-run the full vocabulary suite. DONE: `ForgeVocabularyDerivedGuardTests` (8 tests, includes `test_rule_b_finds_no_target_vocabulary_in_the_forge`) green with every new identifier live in code and docs — the authoritative verdict, not the hand-executed derivation.
- [x] 4.4 Run `npm test`. DONE: **385 tests, pass 385, fail 0** — matches the pre-change baseline exactly.
- [x] 4.5 Full-repo verification command. RAN: `.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` (six suites) and `npm test` — both green. See the apply session's own report for exact counts.
