---
description: "Trigger: audit a skill, a CLI, or any subject that enumerates a closed set — accepted operations, subcommands, error codes, shipped assets — for the gap between what the running code accepts and what its own documentation claims."
---

Read `skills/skill-audit/SKILL.md` and follow it for this task. Treat the text below as `$ARGUMENTS`:

$ARGUMENTS
