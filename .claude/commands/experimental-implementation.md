---
description: "Trigger: turn the latest experiments protocol into working Python against a target repository, run its declared measurements, and verify an existing implementation's layout and revision fidelity against both the experiments protocol and the mathematical proposal it answers to."
---

Read `skills/experimental-implementation/SKILL.md` and follow it for this task. Treat the text below as `$ARGUMENTS`:

$ARGUMENTS
