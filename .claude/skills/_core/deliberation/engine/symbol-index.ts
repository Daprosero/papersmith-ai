import type { StructuralIndex, SymbolIndex } from './types.js';
export function buildSymbolIndex(source:string,index:StructuralIndex):SymbolIndex { const symbols:SymbolIndex['symbols']={}; for(const e of index.entries){const text=Buffer.from(source).subarray(e.startByte,e.endByte).toString(); for(const m of text.matchAll(/(?:^|[^\\])([A-Za-z][A-Za-z0-9_]*|\\[A-Za-z]+)\s*(?:=|\\in|:)/g)){const name=m[1], current=symbols[name]??={normalized:name.replace(/^\\/,'').toLowerCase(),firstEntryId:e.entryId,uses:[],section:e.headingPath};current.uses.push(e.entryId);if(!current.definition) current.definition=text.slice(Math.max(0,m.index!-80),m.index!+100);symbols[name]=current;}} return {symbols,conflicts:[]}; }
