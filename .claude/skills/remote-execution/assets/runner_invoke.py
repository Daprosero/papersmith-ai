#!/usr/bin/env python3
"""PLACEHOLDER — cell 1 of the generated runner notebook.

Provisional stand-in, not the real invoke cell. A later slice (design #744
section 2, "T7b") replaces this content in place with the real,
service-blind invocation: resolving `run.module`/`run.function`/
`run.kwargs` (or the `smoke` variant when `run_config["mode"] == "smoke"`)
through `importlib` and calling it. Copied byte-for-byte alongside
`runner_bootstrap.py`; see that file's own placeholder note.
"""
from __future__ import annotations

raise NotImplementedError(
    "runner_invoke.py is a placeholder; a later slice replaces this with "
    "the real invoke implementation"
)
